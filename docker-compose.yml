version: '3.3'

services:

  app:
    image: linuxserver/calibre-web:0.6.19
    restart: always
    volumes:
      - books:/books
    environment:
      - SSL_POLICY=Mozilla-Modern
      - VIRTUAL_PORT=8083
    networks:
      frontend:

networks:
  frontend:

volumes:
  books:
